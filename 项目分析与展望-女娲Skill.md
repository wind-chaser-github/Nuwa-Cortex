# 项目分析与展望：GuideSkill

本文档旨在全面分析“GuideSkill”项目，记录对该项目核心原理的理解、其潜在的局限与改进方向，并重点深剖其自动化的数据获取途径、搜索逻辑及底层工具链。

## 1. 核心原理：从“人机协作”到“自主AI Agent”

“GuideSkill”的核心并非一个需要人类专家手动操作的研究框架，而是一个**高度自动化的自主AI Agent**。

它的工作原理是“**用一个Agent去创造另一个Agent**”：
1.  **输入**：用户提供一个目标人物的名字（如“费曼”）。
2.  **执行**：“Guide”主Agent被激活，作为“项目总监”自主规划并执行知识工作流。
3.  **过程**：启动多个并行的子Agent（如“著作分析Agent”、“访谈分析Agent”），自动搜集、清洗、分析海量信息，并根据内部模拟专家判断的复杂算法（如“三重验证”算法）提炼目标人物的核心心智模型。
4.  **输出**：最终自动生成一个新的、独立的、可运行的AI Agent（如 `feynman-perspective.skill`），封装了目标人物的思维模式。

## 2. 技术栈

项目巧妙融合了多种技术，其核心在于：
- **自主AI Agent (Autonomous AI Agent)**：“Guide”能进行多步推理、规划和调度执行。
- **算法化的专家启发式 (Algorithmic Heuristics)**：将人类专家的“研究品味”转变为可计算的算法规则。
- **结构化Prompt工程**：生成的 `SKILL.md` 是高度结构化的Prompt，作为新Agent的“操作系统”。
- **生态工具链联动**：通过Shell/Python脚本及调用其他专业Skill，完成复杂的非结构化数据处理。

## 3. 核心机制深剖：数据获取与搜索逻辑

“Guide”并没有一个静态写死的网站列表，而是采用 **“动态搜索” + “规则过滤” + “专项工具分发”** 的高度智能化抓取逻辑。

### 3.1 动态搜索与过滤逻辑

1.  **动态生成搜索词 (Dynamic Search)**：Agent根据目标人物和6个研究维度（著作、对话、表达等），自动组合生成搜索词集（例如：“Richard Feynman interview transcript”、“理查德·费曼 核心观点”）。
2.  **调用基础WebSearch**：通过通用的搜索引擎网络接口获取大量初步的URL列表。
3.  **规则过滤系统 (Rule-based Filtering)**：拿到URL后，通过内部硬编码的规则进行质量筛查：
    - **黑名单（直接丢弃）**：过滤掉高失真、洗稿严重的站点，如 **知乎、微信公众号、百度百科** 等。
    - **白名单与提权**：对权威媒体和优质平台（如 **晚点LatePost、36氪、YouTube、B站**）提高其数据权重。

### 3.2 具体信息源矩阵

在动态搜索的框架下，Agent主要从以下几类平台获取资源：
- **社交媒体与碎片表达**：Twitter/X (西方人物首选)、微博、即刻，抓取人物的高频用词和即时观点。
- **视频与播客平台**：YouTube (全网视频/AMA)、B站 (中国人物原始视频)、小宇宙/喜马拉雅 (深度长音频)。
- **权威媒体与深度阅读**：36氪、极客公园、晚点LatePost、财新、第一财经、虎嗅、少数派、机器之心等。
- **著作与他者评价**：Amazon书评、论文平台、个人博客/Newsletter 存档站。

## 4. 完整的专用工具链配置

在确定了高质量的URL或资源后，“Guide”会像智能调度台一样，分发给最合适的**专用工具**进行数据抓取，而非仅靠通用爬虫。

### 4.1 项目自带的本地脚本 (位于 `scripts/` 目录)
- **`download_subtitles.sh`**：YouTube 字幕提取器。针对视频 URL 自动下载人工或自动生成的 SRT/VTT 字幕。
- **`srt_to_transcript.py`**：字幕清洗机。专门处理 SRT 文件，去除时间戳和 HTML 标签，转化为纯文本逐字稿。
- **`merge_research.py`**：调研汇总器。自动扫描并合并 6 个维度的调研文件，统计信源数量和可靠度，生成验收摘要。
- **`quality_check.py`**：质量巡检员。最终检查生成的 Skill 是否满足心智模型数量、诚实边界等 6 项硬性指标。

### 4.2 依赖的外部专业 Skill (核心能力插件)
“Guide”会自动扫描本地环境，若安装了以下专业 Skill，将优先调用以极大提升数据获取能力：
- **`agent-reach`**：多平台抓取神器。支持 X/Twitter、Reddit、YouTube、LinkedIn 等 17 个平台的深度精准抓取，突破反爬限制。
- **`gemini-video`**：视频解析引擎。用于处理用户提供的本地无字幕视频。
- **`web-article-reader`**：网页净读工具。精确提取复杂网页中的文章正文，剔除广告干扰。
- **`pdf`**：PDF 文档阅读器。解析书籍、研报等一手素材。
- **`huashu-research`**：结构化调研插件。用于对某个细分维度进行深度挖掘。

## 5. 项目局限与后续改进方向

### 5.1 修正后的局限性分析 (算法与生态层面)
- **算法的偏见**：“三重验证”算法倾向于寻找逻辑自洽、反复出现的观点，可能过滤掉人物充满矛盾的真实表达或突发的灵感火花。
- **自动化的脆弱性**：高度依赖 YouTube 等外部平台接口及外部 Skill 生态，一旦平台结构大改，提取脚本可能失效。
- **静态快照**：生成的 Skill 基于历史数据，无法随人物未来的思想演进而自动同步（需重新触发更新流程）。

### 5.2 后续改进方向
- **引入动态检索（RAG）**：让最终生成的Skill能在运行时动态检索人物的原始著作资料库，使回答有据可查。
- **信源权重智能化**：进一步升级算法，让Agent能自主分析判断一个新网站的权威性，而非仅依赖预设的白名单。
- **自我博弈进化**：引入对抗Agent，对生成的Skill进行“压力测试”，发现破绽后强制主Agent重新修改心智模型参数，实现自我迭代。

## 6. 启发与思想：知识工作“Agent化”的未来

“GuideSkill”开创了一种**将高级知识工作“Agent化”的工程范式**。
- **核心启发**：不仅是Prompt，更是“**隐性知识的算法化**”。它展示了如何将人类专家的“品味”、“直觉”（如判别信息真伪、归纳思想主旨）拆解为计算机可执行的步骤流和验证规则。
- **拓展场景**：在此逻辑下，我们可以开发出自动阅读财报生成风格化研报的“投研Agent”，或是自动分析多方庭审记录提取争议焦点的“法务Agent”。只要有清晰的方法论，万物皆可被“蒸馏”。