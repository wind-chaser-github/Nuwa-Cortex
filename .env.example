# Guide Cortex 环境变量
# 复制此文件为 .env 并填入你的 Key：
#   cp .env.example .env
#
# Gateway 启动时会自动读取（无需手动 export）

# 火山引擎 Doubao（当前 config 默认 provider）
ARK_API_KEY=

# 生图（可选，与对话 Key 分开；需 config 里 tools.imageGeneration.enabled=true）
# OPENROUTER_API_KEY=
